# 📑 LiveMarksTab — Chrome 新标签页书签导航

> 打造极致美观、高效实用的 Chrome 新标签页，将浏览器书签自动渲染为现代化导航界面。

---

## ✨ 核心特性

### 🎨 极致设计
- **现代化界面**：采用毛玻璃特效、平滑动画与精心调配的色彩，提供卓越的视觉体验。
- **三种布局模式**：
  - **卡片瀑布流 (Masonry)**：美观且高效，自动适配屏幕宽度，像 Pinterest 一样展示书签文件夹。
  - **分行布局 (Grid)**：经典的网格排列，清晰直观。
  - **侧边栏布局 (Sidebar)**：垂直导航，适合书签分类较多的用户。
- **深色模式支持**：自动跟随系统或手动切换，支持自定义背景图片（网络图片/本地上传/渐变色）。

### 🚀 高效管理
- **双向实时同步**：书签的增删改查实时同步到 Chrome 书签库，反之亦然。内置 Sync Lock 防止循环更新。
- **拖拽排序**：支持文件夹和书签的自由拖拽排序，所见即所得。
- **右键菜单**：提供打开、编辑、删除、**生成二维码**（支持复制图片/URL）等便捷操作。
- **书签清理**：内置强大的清理工具，一键扫描失效链接、空文件夹，支持批量清理，保持书签库整洁。

### 🔍 搜索与导航
- **聚合搜索**：集成 Google, Bing, 百度, DuckDuckGo, GitHub 等多个搜索引擎。
- **实时过滤**：在搜索框输入关键词，即时筛选显示匹配的书签。
- **快捷键支持**：`Shift+Enter` 可同时在选中的多个搜索引擎中搜索。

---

## � 安装指南

本项目目前通过 **Chrome 开发者模式** 安装。

1.  **下载代码**：
    -   `git clone https://github.com/lemonsuan/LiveMarksTab.git`
    -   或者下载 ZIP 压缩包并解压。

2.  **打开扩展管理页**：
    -   在 Chrome 地址栏输入 `chrome://extensions/` 并回车。
    -   或者点击浏览器右上角菜单 -> 更多工具 -> 扩展程序。

3.  **开启开发者模式**：
    -   打开页面右上角的 **"开发者模式" (Developer mode)** 开关。

4.  **加载扩展**：
    -   点击左上角的 **"加载已解压的扩展程序" (Load unpacked)** 按钮。
    -   选择本项目文件夹 (`LiveMarksTab`)。

5.  **启用**：
    -   确保扩展已启用。现在打开一个新的标签页 (`Cmd/Ctrl + T`)，即可看到 LiveMarksTab 界面。

---

## 📖 使用说明

### 切换布局与主题
-   点击页面右上角的 **设置图标 (⚙️)** 打开设置面板。
-   在 **"布局设置"** 中选择喜欢的布局（瀑布流/分行/侧边栏）。
-   在 **"外观设置"** 中切换深色/浅色模式，或上传自定义背景图。

### 管理书签
-   **添加/编辑/删除**：直接在书签卡片上右键单击，选择相应操作。
-   **排序**：长按书签或文件夹标题栏，拖拽到新位置即可。

### 清理书签
-   点击右下角的 **扫帚图标 (🧹)** 进入清理模式。
-   点击 **"开始扫描"**，系统将检测所有书签的有效性。
-   扫描完成后，勾选失效书签并点击 **"清理选中"**。

---

## �️ 技术栈

-   **核心**：原生 JavaScript (ES6+)，无繁重框架依赖，追求极致性能与秒开速度。
-   **样式**：CSS Variables (动态主题) + Flexbox/Grid (响应式布局)。
-   **库**：`qrcode-generator` (轻量级二维码生成)。

---

## � 权限说明

为了提供完整功能，本扩展需要以下权限：

| 权限 | 用途 |
| :--- | :--- |
| `bookmarks` | 读取和修改书签，实现导航页与浏览器数据的同步。 |
| `favicon` | 获取网站图标，美化书签显示。 |
| `storage` | 保存您的布局偏好、主题设置等本地配置。 |
| `host_permissions` | 用于"书签清理"功能中检测链接是否有效 (发送 HEAD 请求)。 |

---

## 🤝 贡献

欢迎提交 Issue 或 Pull Request 来改进本项目！

## � 许可证

MIT License
